import OpenAI from 'openai';
import { CopilotKitServiceAdapter, CopilotKitResponse } from '../types/service-adapter.js';
import { Message } from '@copilotkit/shared';

interface OpenAIAssistantAdapterParams {
    assistantId: string;
    openai?: OpenAI;
    codeInterpreterEnabled?: boolean;
    retrievalEnabled?: boolean;
}
declare class OpenAIAssistantAdapter implements CopilotKitServiceAdapter {
    private openai;
    private codeInterpreterEnabled;
    private assistantId;
    private retrievalEnabled;
    constructor(params: OpenAIAssistantAdapterParams);
    waitForRun(run: OpenAI.Beta.Threads.Runs.Run): Promise<OpenAI.Beta.Threads.Runs.Run>;
    submitToolOutputs(threadId: string, runId: string, forwardMessages: Message[]): Promise<OpenAI.Beta.Threads.Runs.Run>;
    submitUserMessage(threadId: string, forwardedProps: any): Promise<OpenAI.Beta.Threads.Runs.Run>;
    getResponse(forwardedProps: any): Promise<CopilotKitResponse>;
}

export { OpenAIAssistantAdapter, OpenAIAssistantAdapterParams };
